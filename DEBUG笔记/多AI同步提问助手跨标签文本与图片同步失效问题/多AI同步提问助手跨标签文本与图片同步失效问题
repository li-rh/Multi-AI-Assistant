> Debug日期：2026-03-28  
> 最后验证结果：未完全修复（引入两阶段注入观察，待多站测试排查遗留报错）

**终端摘要（本次以功能失效为主，无明确编译/运行报错）**
- 无构建报错；核心问题表现为：`文本未自动同步发送`、`图片偶发双图`、`非图片文件未同步注入`。
- 代码层面经历多次演进：发送兜底 -> 内容去重(`v3.2`) -> 全局策略化与两阶段验证(`v3.3-dev`)。

## 关键报错摘要
- 初始阶段：`文本同步不触发`（仅依赖网络拦截时，部分站点发送链路变化导致漏抓）。
- 初始阶段：`图片同步不生效`（文件输入元素可用性判断过严，隐藏 file input 无法命中）。
- 中期修复后：`图片偶发双重广播`（部分站点先 paste 后由于站点自身操作再引发 change 事件，未阻断）。
- v3.2/v3.3 尝试：`非图片文件不同步`（因防重禁用了 file input 导致正常文件受牵连），`注入状态判断失效`（误将 `dispatchEvent` 返回 true 视为注水成功）。

## 一、现象与报错演进
- 步骤1：基于融合脚本验证，发现文本同步与图片同步均未稳定生效。
- 步骤2：第一轮改造后，文本同步在部分站点恢复，但图片/文件在若干站点仍失败。
- 步骤3：复查后确认资产注入优先级与输入元素筛选策略存在误差。
- 步骤4：形成双通道闭环（文本通道、资产通道各自可独立工作）。
- 步骤5：实测中发现图片偶发双重注入，追加了 dataUrl 内容去重和较长的抑制窗口（v3.2）。
- 步骤6：再暴露新回归，部分站点（如元宝/通义）非图片文件无法同步，即被“防图片多发”规则误伤。
- 步骤7：引入策略 B，实施两阶段注入验证，延迟核对 DOM 快照（v3.3-dev），分离了图片与非图片的上传策略。

## 二、根因拆解
1. 文本触发源单一（已修复）
- 仅靠 `XHR/fetch` 请求拦截提取 query，在站点改版或请求体不可读时会漏抓。

2. 资产注入策略偏差 / 文件过滤过严（已优化）
- 先走 `input[type=file]` 且限制“可见”，导致隐藏但有效的 file input 被排除。放过宽又导致多 input 被误用。需要对 `accept` 与元素可见性智能 fallback。

3. 任务锁耦合（已修复）
- 文本与资产共用处理状态，出现处理互斥导致偶发跳过。

4. 图像上传回环与内部多发（已修复机制引入）
- 源端部分站点粘贴时会同时派发 `paste` 与 `change`，引发“双重捕获”。通过短时间内容 Hash（`dataUrl fingerprint`）成功滤除。

5. 盲信 `dispatchEvent` 返回值作为回退依据（已发现，正测试修复）
- 返回 true 仅表示事件未被彻底阻止，不表示站点业务确实处理并挂载了附件，从而过早中断了 file input 的后续回退。需改用前后 DOM 附件标记比对。

6. 图片与普通文件策略混用（已修复机制引入）
- 在配置 `TONGYI` 站点为免除 file input 后，普通文件也惨遭屏蔽。

## 三、关键对照实验（定位依据）
1. 触发源对照：网络拦截 vs 本地事件兜底 -> 必须并用。
2. 注入顺序对照：先 file input vs 先 paste -> 应当对图片优先 paste。
3. 注入是否成功判断对照：`dispatchEvent返回值` vs `前后DOM附件快照数对比`
- 对照：仅看返回值常出现误判不回退问题。引入 `takeAssetDomSnapshot` 看 `blob:` 附件数是否增加。
4. 资源控制类：图片同步策略 vs 非图片同步策略 -> 必须分离，非图片文件不能被防双图规则禁封。

## 四、最终修复方案
1. 在 `comms` 中新增本地发送兜底（网络监听异常时重补）。
2. 调整资产注入主流程并分流：
- 图片：受 `enablePasteForImage` 等字段控制，先 paste 并经过快照前后对比验证失败后，再 fallback到 file input (`enableFileInputFallbackForImage`)。
- 通用文件：受 `enableFileInputForNonImage` 控制，通过 file input 直接上传。
3. 放宽文件输入查找策略，并利用 `findUploadTrigger` 支持去寻找需预先点击按钮才能激活的隐藏 file input 组件。
4. 基于 `takeAssetDomSnapshot` 两阶段对比 DOM 中含 `blob:` 等附件特征，不笃信 event dispatch。
5. 增加发送与注入的双流隔离锁 (`isProcessingQueryTask`, `isProcessingAssetTask`) 与 `makeDataUrlFingerprint` 抑制源端由于站点侧重发事件而导致的跨站连带重复重发问题。

## 五、当前状态
- 代码层面：已实施落地方案 B（两阶段注入、解耦图片与文件上传规则、重构组件获取）。
- 前置测试：已提供专门回归用例（A/B/C 三路组合验证：通义、元宝的图片与非图片资产双向流转）。
- 最终联调：基于未完全解决现状，等待进一步用户端操作日志对照“校验结果”做下一轮打磨。

## 六、经验沉淀（后续避免）
- 不要把“发送检测”只绑定在网络拦截；必须保留 UI 事件兜底。
- 对于 `dispatchEvent` 或者基于事件触发的前端行为，绝不能把 `isCancelled===false` 当“操作真实落地”，核心判断要落在“业务状态”的变化上，如本业务中通过附件快照的对比解决。
- “防止双图”逻辑不能通过一刀切关掉某些站点的 input 行为，要针对不同类型的文件分类制导。

## 七、归档文件清单
- `多AI同步提问助手跨标签文本与图片同步失效问题`：本次主复盘文档。
- `README.md`：归档索引、速查表与回归测试用例单。
- `CHANGELOG.md`：本归档后续迭代记录。

## 八、复现与验证命令清单
### A. 文件层检查
- 查看最新注入机制点：`Select-String -Path .\多AI同步提问助手.js -Pattern "isAssetLikelyAttached|takeAssetDomSnapshot|discoverFileInputViaTrigger"`

### B. 浏览器手工验证（见 README ）
- 根据 `README.md` 的《v3.3-dev 最小回归脚本》来在 通义 和 元宝 进行专门测试。
